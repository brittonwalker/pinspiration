<h2><%= @user.username %>'s Pins</h2>


<% @pins.each do |pin| %>
<div class="each_pin">
    <%= image_tag pin.img_url, class: "pin_image"%>
  <div class="pin_title">
    <%= pin.title %>
  </div>
</div>
<% end %>
